import React from 'react';
import { View } from 'react-native';

const Card = () => {
    return(
        <View></View>
    );
};

const styles = {
    containerStyle: {
        borderWidth: 1
    }
}

export default Card;